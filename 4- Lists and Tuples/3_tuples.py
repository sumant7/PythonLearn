#tuples is also a container
t = (1,2,3,4)

print(t[0])   #outputs 1
#CANNOT UPDATE   t[0]=34 won't work

#empty tuple
a = ()
print(a)   #outputs ()


#single element tuple requires comma
b=(1,)    # (1) will be treated as an integer inside brackets
print(b)   #outputs (1,)


#once defined tuple can't be altered
#once defined tuple can't be altered